
Too slow output!
	Changed to 100
	
Write to log file every X seconds	
	Buffer is around 8KB

Reload serial ports
	Done

Button for new log file
	Just reconnect

Stop logging if serial port fails
	Done

Show file name in reconnect line
	Shown in disconnect line

Gradient on timedelta

Show settings (max line count)
	Done

Move connect/disconnect lines to buffer (to be able to reload)
	Done
	
#######################
	
Add highlight in GUI

Hide lines (Requires new buffer)
Hide lines button

Search for string

Poll serial connection (detect disconnect)
	
Settings file
	Settings in a JSON file to avoid problems with weird characters

Position of settings/options window
Program should keep working even if options window is open?

Change to classes?